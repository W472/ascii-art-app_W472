# ascii-art-app_W472
This is a web tool that turns images into ASCII art. After users upload an image, the backend processes it into 'ASCII art' made up of characters and sends it back to the frontend for display.
